---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: >-
      helm install minio
      --namespace $NAMESPACE
      --version 15.0.7
      -f 01_helm-bitnami-minio-values.yaml
      oci://registry-1.docker.io/bitnamicharts/minio
    timeout: 240
